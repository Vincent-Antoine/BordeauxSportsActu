<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Bordeaux Sports Actu</title>
</head>
<body>
    <header class="bg-gray-800 text-white">
        <div class="container mx-auto flex justify-between items-center py-4 px-4 lg:px-8">
            <!-- Logo -->
            <a href="{{ path('app_home') }}" class="text-2xl font-bold">Bordeaux Sports Actu</a>

            <!-- Hamburger Menu -->
            <button 
                class="lg:hidden block focus:outline-none"
                data-collapse-toggle="mobile-menu" 
                type="button" 
                aria-controls="mobile-menu" 
                aria-expanded="false" 
                id="menu-toggle"
                aria-label="Toggle navigation">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                </svg>
            </button>

            <!-- Navigation (Desktop) -->
            <nav id="menu" class="hidden lg:flex lg:items-center lg:space-x-6">
                <ul class="lg:flex lg:space-x-6 items-center">
                    <li><a href="{{ path('app_resultats_index') }}" class="hover:text-gray-400 block py-2 lg:py-0">Résultats</a></li>
                    <li><a href="{{ path('match_rugby_results') }}" class="hover:text-gray-400 block py-2 lg:py-0">Rugby</a></li>
                    <li><a href="{{ path('app_live_index') }}" class="hover:text-gray-400 block py-2 lg:py-0">Live</a></li>
                    <li><a href="{{ path('app_blog_index') }}" class="hover:text-gray-400 block py-2 lg:py-0">Blog</a></li>
                    <li><a href="{{ path('app_contact_index') }}" class="hover:text-gray-400 block py-2 lg:py-0">Contact</a></li>
                </ul>

                <!-- Auth Links -->
                <ul class="lg:flex lg:space-x-6 items-center">
                    {% if app.user %}
                        <li>
                            <span class="block py-2 lg:py-0">Bienvenue, {{ app.user.userIdentifier }}</span>
                        </li>
                        <li>
                            <a href="{{ path('app_profile') }}" class="bg-yellow-500 text-white px-4 py-2 rounded hover:bg-yellow-600">
                                Mon profil
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('app_logout') }}" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">
                                Déconnexion
                            </a>
                        </li>
                    {% else %}
                        <li>
                            <a href="{{ path('app_login') }}" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                                Connexion
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('app_register') }}" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">
                                Créer un compte
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </nav>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden lg:hidden bg-gray-700">
            <ul class="space-y-2 py-4 px-4">
                <li><a href="{{ path('app_resultats_index') }}" class="hover:text-gray-400 block">Résultats</a></li>
                <li><a href="{{ path('app_live_index') }}" class="hover:text-gray-400 block">Live</a></li>
                <li><a href="{{ path('app_blog_index') }}" class="hover:text-gray-400 block">Blog</a></li>
                <li><a href="{{ path('app_contact_index') }}" class="hover:text-gray-400 block">Contact</a></li>
                {% if app.user %}
                    <li>
                        <span class="block py-2">Bienvenue, {{ app.user.userIdentifier }}</span>
                    </li>
                    <li>
                        <a href="{{ path('app_profile') }}" class="bg-yellow-500 text-white px-4 py-2 rounded hover:bg-yellow-600">
                            Mon profil
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_logout') }}" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">
                            Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('app_login') }}" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                            Connexion
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_register') }}" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">
                            Créer un compte
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </header>

    <!-- Inclure Flowbite JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.js"></script>
</body>
</html>
